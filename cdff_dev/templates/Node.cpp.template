#include "{{ node["name"] }}.hpp"

namespace dfn_ci {

{{ node["name"] }}::{{ node["name"] }}()
{
}

{{ node["name"] }}::~{{ node["name"] }}()
{
}

bool {{ node["name"] }}::configure() 
{
    // TODO Fill in configure functionality here
    return true;
}


bool {{ node["name"] }}::process() {
    // TODO Fill in process functionality here
    return true;
}

}
